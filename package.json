{
  "name": "urban_dictionary",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    // "start": "cd backend && yarn server && cd ../frontend && yarn start",
    "start": "cd backend && yarn server",
    "server": "cd backend && yarn server"
  },
  "dependencies": {
    "@material-ui/core": "^4.11.4"
  }
}
